---
template: dhcp

# Activate all, or a specific interface
all: true

# Which (list) would you like to modify?
modify_interface: "{{ inactive_interface }}" # || active_interface

# interface_num: 0

debug: false